MadOS API deployed successfully!

  API URL:         https://mados-api.mb-dev.ru
  Supabase Studio: https://mados-supabase.mb-dev.ru
  Image tag:       {{ .Values.image.tag }}

Useful commands:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ .Chart.Name }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ .Chart.Name }} -f --tail=100
  kubectl describe ingress -n {{ .Release.Namespace }} {{ include "api.fullname" . }}
