# Dependencies
node_modules/
.venv/

# Build outputs
dist/
.next/
out/
__pycache__/
*.py[cod]
# Runtime copy of migrations — source of truth is packages/db-backend/src/migrations
apps/api/migrations/

# Turborepo
.turbo/
.turbo

# Environment variables
.env
.env.local
.env.*.local
.local-env/
supabase/.temp/
deploy/.env.prod
deploy/.env.vercel
deploy/.secrets/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Testing
coverage/

# Claude Code local artifacts (logs, screenshots, lock files, per-user state)
.claude/logs/
.claude/screenshots/
.claude/projects/
.claude/memory/
.claude/*.lock
.claude/*.local.json
.claude/settings.local.json
# …but tracked skill scripts under .claude/ ARE checked in.
!.claude/skills/*/scripts/
!.claude/skills/*/scripts/**

# Misc
*.tsbuildinfo
*.log
*.timestamp-*.mjs
.playwright-mcp/

# Local dev artifacts
.pnpm-store/
.tmp/
.worktrees/
_qry.cjs
.test-requests.*

.vercel
.env*
# ...but per-package example env files ARE tracked (the scaffolder copies them).
!.env.example
!**/.env.example

# Local demo runtime (stable ngrok tunnel + local API) — see docs/deployment.md
.demo.env
.tools/
demo-state/
# Untracked local secrets / superseded helper scripts (never commit)
api-secrets.yaml
vercel-env-preview.txt
run-api-local.ps1
cloudflared.exe
ngrok.yml
# Assignment brief
*.docx

# Local-only engineering notes (e.g. internal backlogs) — never committed
*.local.md

# Superseded one-off helper scripts (kept on disk, never committed — replaced by
# bootstrap.ts CORS config and `make demo`)
cors-patch.js
patch-cors.js
patch-cors.sh
test-ollama*.sh
